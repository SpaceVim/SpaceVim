let prof_fname = get(g:, 'prof_fname')
exe 'profile start '.prof_fname
profile! file autoload/SpaceVim/*
filetype off
set rtp+=build/vader
set rtp+=.
set rtp+=after
filetype plugin indent on
syntax enable
set nocompatible
